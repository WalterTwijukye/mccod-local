import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.medicalcertificateofcauseofdeath',
  appName: 'MedicalCertificateOfCauseOfDeath',
  webDir: 'SK_dhisForm',
  server: {
    androidScheme: 'https'
  }
};

export default config;
